Provide a sample application implementing with a UI based on graphqlite using https://colliery-io.github.io/graphqlite/latest/ and https://github.com/colliery-io/graphqlite and more specifically an implementation with my local ollama through a UI, with mock data or the possibility to add adequate data.
If a database is needed other than the ones provided or explained in the samples and documentation you can provision a sqlite database (only if needed).
Documents should be created in "Docs" folder except for the global readme.md
Build a memrmaid architecture flow document as well.
All scripts, if needed should be in "scripts" folder.
I need a script to lauch the application in detached mode showing the application's URL.
I need a script to stop the application. This script should also for the firt usage initialize the python venv and install any required packages through the requirements.txt. 
Do not use port 5000 if needed, already in use, propose an altrenative port.
I need a github-push script which will do all the github repository, the only input parameters are the repository URL and the commit commands.
Input data if needed in "input", and "output" in "output" folder.
When you make changes to the application of any other component, always update the existing documents and scripts, avoid creating new one.
Test your code and scripts.
Always refer back to this context.md document to verify if your work is based on these requirements.